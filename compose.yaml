version: '3.8'

services:
  analisis-jni:
    image: 'juandiiego2111/analisis-jni:latest'
    ports:
      - '8084:8080'
    networks:
      - red-backend-app
networks:
  red-backend-app:
    driver: bridge